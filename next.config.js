/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "source.unsplash.com",
      "images.unsplash.com",
      "upload.wikimedia.org",
    ],
  },
};

module.exports = nextConfig;
